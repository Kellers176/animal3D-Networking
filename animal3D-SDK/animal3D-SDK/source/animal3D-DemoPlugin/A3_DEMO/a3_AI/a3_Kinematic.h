

#ifndef KINEMATIC_H
#define KINEMATIC_H

struct Kinematic
{
	// position // vec 2 or vec3
	// orientation // single floating val
	// velocity // another vec 2 or vec3
	// rotation // a single floating value

	/*
	def update( steering, maxSpeed, deltaTime)
	{
		// update teh position and orientation
		// position += velocity * deltaTime
		// orientation += steering.angulat * deltaTime

		// check for speeding and clip
		// if velocity.length > maxSpeed
		// {
		//		velocity.normalize();
		//		velocity *= maxSpeed;
		// }
	}
	*/
};

#endif